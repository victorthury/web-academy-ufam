# Node 3

## Criar `.env`

Crie o arquivo `.env` a partir do que está contido em `.env.example`

## Inicialização

Rode o comando `npm install` para instalar as dependências

## Execução

Para rodar, execute `npm start`.
