# Instruções

## Criar `.env.development` e `.env.production`

Crie os arquivos acima a partir do que está contido em `.env.example`

## Inicialização

Rode o comando `npm install` para instalar as dependências

## Execução

Para rodar em development, execute `npm start`. Para rodar em production,
rode `npm run start:prod`
